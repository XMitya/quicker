pluginVersion=0.1.3
# 2025.2.3 == build 252.x — last release with a genuine Community distribution, so compiling
# against IC is a mechanical guarantee that no Ultimate-only API is used. See plan R5.
platformVersion=2025.2.3

org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g
org.gradle.caching=true
org.gradle.parallel=true
kotlin.stdlib.default.dependency=false
